# Contributing

Install Node.js 20 or newer, then run:

```sh
npm ci
npm test
npm run typecheck
```

Keep changes focused, preserve the existing TypeScript ESM setup, and update
tests and documentation when behavior changes.
